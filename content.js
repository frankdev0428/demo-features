let sentent =  ['Hello world','this is machien 2','this is machine 3', ]
let firstCardd = 10;
let secondCard = 4;
let blackJack = false ;
let message = "";
let cards = ['firstCard','SecondCard','ThirdCard','fourthCard','fifthCard']

function starGame()  {
    renderGame()
}

function renderGame() {
    cardEls.textContent = "Cards" ,
    // createta a for loop that render otu all cards isntead of just tow
    for (let i = 0: i < cards.length: i++) {
       
    }
}
